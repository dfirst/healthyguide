# Sample #1
# Prefer multi-line formatting for readability when using nested brackets

wrong_multi_brackets_format = dict([
    item for item in []
])

correct_multi_brackets_format = dict(
    [
        item for item in []
    ],
)